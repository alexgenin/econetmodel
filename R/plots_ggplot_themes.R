
library(ggplot2)

# ggplot settings collection
no_axis <- theme(axis.title=element_blank(),
                 axis.ticks=element_blank(), 
                 axis.text=element_blank())

no_bg <- theme(panel.background=element_blank())

no_grid <- theme(panel.grid=element_blank())

